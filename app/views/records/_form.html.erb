<%= form_for(@record) do |f| %>
  <% if @record.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@record.errors.count, "error") %> prohibited this record from being saved:</h2>

      <ul>
      <% @record.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :resquest_id %><br>
    <%= f.number_field :resquest_id %>
  </div>
  <div class="field">
    <%= f.label :applicant_id %><br>
    <%= f.number_field :applicant_id %>
  </div>
  <div class="field">
    <%= f.label :volunteer_id %><br>
    <%= f.number_field :volunteer_id %>
  </div>
  <div class="field">
    <%= f.label :status %><br>
    <%= f.check_box :status %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
